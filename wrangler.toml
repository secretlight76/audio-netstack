name = "audio-network-visualizer"
compatibility_date = "2024-01-01"

# Configuration pour Cloudflare Pages
# Note: La section [build] n'est PAS supportée pour Pages
# Utilisez .cloudflare/pages.json ou le Dashboard pour la config build
pages_build_output_dir = "dist"

# Variables d'environnement
[vars]
ENVIRONMENT = "production"
